# generated data
route_data.json

# input GPX files (keep the dir, ignore the files)
input/*.gpx

# node / OS
node_modules/
.npm-cache/
.DS_Store
.local/

# trailpilot server
server/target/
data/
cache/
.toolchain/
# user GPX data dropped into the workspace — do not publish
squirrel camp 2026.gpx
